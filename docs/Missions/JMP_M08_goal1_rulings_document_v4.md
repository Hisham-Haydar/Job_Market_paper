# JMP Goal-1 Consolidated Rulings Document — R-59 … R-80 (v1)
**Programme:** Goal 1 — Empirical JMP. **Class:** Manager governance record
(written primary text for rulings previously existing only in the Goal-1
Manager chat). NOT deputy text. Issued 2026-08-07 to close the auditability
gap recorded in JMP_M08_goal1_rulings_register_v1.md; supplies the written
R-64 required at contract §2.1 and the written ratification of ε_Shapley
(R-60.5, R-76.2) required by Stage-A review finding A6.1. Where this summary
and a binding deputy instrument differ, the deputy instrument controls.

- **R-59** (2026-08-05). M08 register rulings: U1 digests supplied for
  verification; U2 non-employment key extracted from committed source; U3 =
  chat-side provenance note, no repo artifact; U4 one targeted re-search else
  deputy escalation; U5–U8 adopt documented Stage-One values, absent values
  PROPOSED-PENDING-RATIFICATION never silently frozen; U9 namespace
  outputs/p2a_singles2016/region_live_v1/welfare_m08_v1/ with attempts/;
  U11/U13/U14 scaffold defaults ratified (Ā = type_conditional_median_
  opportunity; R_h; held); U15 binding text = deputy LOC4 Path-B ruling.
- **R-60** (2026-08-06). Parity repair Route 6→Route 1 ratified: gate binds
  the committed hash-pinned P2a pricing cache at target-only D-BEN Option B
  geometry; correction scope = gate/harness only. U2 schema correction
  (predicate, not key). **U7 FROZEN: 0.5 nats** for the |V_dir − V_IS|
  cross-check (Stage-One code constant). **U8 FROZEN (convention class):
  ε = 1e-9·max(1,|I(Ω)|)** for Shapley numerical-identity checks — a
  numerical convention analogous to the finite-sample correction, not a
  scientific threshold. U15 canonicalization; U4 escalated with proposed list.
- **R-61** (2026-08-05). En-dash FAIL reclassified as verification-reference
  defect; files stood; standing rule 8 (byte-gates bind source artifacts or
  annotate code points); error log → 6.
- **R-62** (2026-08-06). M07I immutable-history set committed; deputy
  closeout ruling moved to canonical docs/Missions/; project-state file
  report-only pending provenance.
- **R-63** (2026-08-06). Review-artifact front-matter class exemption; R100
  rename accepted; **.3:** project-state referent pinned sha256 c567f443…6588,
  physical placement deferred to a documentation checkpoint.
- **R-64** (2026-08-06). Stage-B parity PASS accepted (later suspended by
  R-66 pending review): attempt …_PARITY_PASS_FULL evidence-of-record;
  display-shim disposition in-scope; witness limitation disclosed-adequate;
  carry-forwards: FR.xml timing = attribution not causation; **joint batching
  unlicensed — target-only Option B geometry for any node pricing**; U3
  closed; U10 mechanical-at-freeze; MNL commit deferred behind review.
- **R-65** (2026-08-06). Deputy U4 subgroup ruling adopted and placed;
  W¹–W⁶ bound to the charter's frozen measure list; weighting-convention
  halt armed; addendum v2 substitution under deputy license.
- **R-66** (2026-08-06). Codex production-path REJECT (T4/T7,
  implementation-only) accepted; R-64 PASS acceptance suspended; the single
  rule-3 conversion invoked and spent (comparator fix + certified re-run +
  report v2); certification standard tightened to tolerance-AND-finiteness;
  re-verification REJECT pre-registered as E2.
- **R-67** (2026-08-06). Reviewer's verbatim clause substitution executed as
  manager-verified edit (disclosed route, no drafting judgement); ChatGPT
  Pro Thinking adopted as default tier for future independent reviews.
- **R-68** (2026-08-06). Conversion return accepted; witness-row capture
  retained (evidence-additive, non-gating, disclosed to reviewer); EUROMOD
  uprate notices non-actionable; AC-5R re-issued citing the certified attempt.
- **R-69** (2026-08-06). M07I rider closed; tran-landed governance files to
  history; v3-memo front-matter path fix = output-discipline; U4 path watch.
- **R-70** (2026-08-06). E2 declared per R-66.2. Error log → 7; standing
  rule 8b (canonical placement by file transfer + hash, never retyped
  blocks). AC-5R resolutions frozen: dwt = db090 headline weighting
  (population statistics weighted, estimator unweighted); educ3 = {0,1,2}
  with educ3=−1 assert-halt; drgn1 ∈ {1..8} code-labels-only, ref cat 1,
  no DEFAULT_NUTS1_ORDER naming; charter six-measure list overrides the
  four-measure yaml; unweighted _quantiles/gini prohibited. U3 closed; U15
  resolved-on-disk; U7/U8 as R-60. Rulings register mandated. **U6 ruled:**
  2×/4× draw-dataset creation authorized as Stage-D work; Gate-1(i)
  tolerance PROPOSED-at-freeze from Stage-One diagnostics, manager-ratified
  pre-Stage-D. U12 commissioned. Repo merger deferred (deputy later ratified).
- **R-71** (2026-08-06). U12 access-operand memo ratified: D1–D10 (D1 =
  factor×argument-cell routing, scaffold §7 amendment); R4 share-weighted
  index baseline + one sensitivity; R5 ω_s = within-educ3 dwt sex shares;
  R6 all operand population references dwt-weighted; R7 V1–V13 with
  tolerances (V1 1e-12; hash/bitwise exact classes; V8 tol = U8 convention);
  **V_i^dir declared UNBLOCKED by certified parity and mandatory at Stage D
  on flagged subsets at 0.5 nats**; R8 verify-then-escalate; R9 mirror memo
  commissioned; R10/R11 ratified.
- **R-72** (2026-08-06/07). R9 mirror memo ratified: B1–B6, P1'–P4', C1.
  S1 index-mean convention + single sensitivity. **S2: θ̄^pref = singles-
  female accepted block; singles-male block = mandatory pre-registered
  mirror sensitivity; recorded under V23 before any coalition value.**
  S3 education-in-leisure → preference (consequence disclosed). S4 V20a/b/c
  split (V20b tolerance Stage-A-proposed). S5 opportunity ≡ C_A+C_B (+V21).
  S6 folded into R8 verify-then-escalate. S7 V14–V23 ratified. S8 Tier-2
  expected-positive posture (paper-claims level). S9 specification-limits
  caveat block.
- **R-73** (2026-08-06). Deputy E2 documentary ruling + five strategic
  decisions adopted (merger deferral; notebook freeze + v3 successor;
  package pin; documentation proportionality — R-69.2 partially reversed,
  prompts file deleted; routing-not-governance).
- **R-74** (2026-08-06). Report v3 correction accepted; verification
  released to run parallel.
- **R-75** (2026-08-07). AC-12 valid E2-scope REJECT (E2-2 residual);
  charter design conflict (W³ degeneracy; c_ij residual) escalated with the
  endowment/needs fourth-channel candidate; error log → 8 (rulings
  auditability gap — this document is the remedy); U15 closed; HEAD rebind.
- **R-76** (2026-08-07). Deputy final E2 + decomposition-architecture
  ruling adopted verbatim (W2 primary / W5 secondary / W3 validation-only;
  three-channel game; R_bg accounting; five §3.5 gates; s_opp with the 2pp
  S-10 trigger; literature freeze). Survival mappings: R-72.S5 re-anchored
  to W2; V8/V20a-b-c superseded — V20a survives as gate §3.5(1)'s
  mechanical form, V20b retired, V20c survives as gate §3.5(4)'s
  enumeration; V21 diagnostic; fixed points re-anchored to W2/W5.
  **ε_Shapley for the §3.5 arithmetic and total-accounting gates =
  the U8 frozen convention 1e-9·max(1,|I^k(∅)|) — hereby the written
  Goal-1 ratification (with R-60.5) that contract v3 cites.**
- **R-77** (2026-08-07). Final E2 verification ACCEPT; **parity axis
  FROZEN**; v4 = report of record; MNL parity checkpoint commit authorized.
- **R-78** (2026-08-07). Tran cleanup rules (repos authoritative; hash
  dedup; copy-only-if-missing); AC-14 flags dispositioned — U3 stays
  ESCALATED-NO-REFERENT disclosed to review; **U7 contradiction resolved in
  favour of the V22/R-71 transcription (0.5 nats frozen)**; pin asserted at
  commit; R-64 written text = this document.
- **R-79** (2026-08-07). Parity closed at MNL 5b0e3d29; tran cleanup
  accepted; two git-mv relocations queued (S-10 spec → docs/Missions;
  positioning memo v2 → docs); Stage-A review commissioned.
- **R-80** (2026-08-07). Stage-A review PASS-WITH-4-CORRECTIONS accepted;
  review memo retained at docs/Missions/JMP_M08_stageA_contract_review_v1.md;
  single correction cycle authorized → contract v3; A6.1 cured by this
  document (R-60.5 + R-76.2); V6/V22/U6 numbers remain under explicit
  execution block, ratified on Stage-A proposals pre-Stage-D; narrow
  four-item confirmation in the same review chat before freeze.

No welfare number, decomposition number, parameter value, or estimation is
produced or implied. This document records; it does not newly rule.

---

## Appended in v2 — R-81 … R-86 (2026-08-07 / 2026-08-08)

*Frame note, so the inconsistency is declared rather than silent: everything
above this heading is **v1's full text, byte-identical** (sha256
`e663802712b2e004711beb327432a43e64501a47d82fcd7d6a2589c3e689ae2d`), copied by
file transfer under standing rule 8b and **not retyped or edited** — including
its title line, whose `R-59 … R-80 (v1)` range and version marker are therefore
**stale on their face and are superseded by this appendix**. The register this
document carries now runs **R-59 … R-86**. The class, the authorship
(Goal-1 Manager governance record, NOT deputy text), the precedence rule
(*"Where this summary and a binding deputy instrument differ, the deputy
instrument controls"*), and the closing discipline paragraph above all apply
unchanged to the entries below.*

- **R-81** (2026-08-07). Theory paper (Maniquet) admitted as the
  definitional citation source for W1-W6: definitions and reference
  constructions extractable and citable; axioms/theorems/proofs cited
  by existence only; no JMP claim of the axiomatic contribution.
  Measure-definition binding memo commissioned.
- **R-82** (2026-08-07). Contract v3 accepted pending confirmation;
  the five coordinate-empty cells adopted as extraction facts
  (caveat-block disclosure; operators unchanged at argument/table
  level; V13 load-bearing; R-72.S3 recorded vacuous-in-spec); s8.4
  staleness routed to freeze discharge.
- **R-83** (2026-08-07). AC-20 K2 residue = incomplete execution of
  the authorized correction, completed as v4 (not a deputy return;
  halt-condition amendment disclosed). Error log 9. Standing practice
  9: every correction instruction carries a whole-document residue
  scan for the corrected concept.
- **R-84** (2026-08-07). Measure-binding memo accepted: U13 R^h
  citation ratified (measure-side only, no computational role in the
  W5 number); O-8 (home_count >= 1 blocking assertion) and O-9
  (manuscript wording) ratified; O-2/O-4/O-5/O-6/O-7 routed to the PI
  as theory-source items; O-3 domain reading proposed; O-1
  escalated to the PI with recommendation A.
- **R-85** (2026-08-07). v4's s10.6 residue-scan record retroactively
  authorized as revision-record content (frame error owned, log 10);
  K2 substance confirmed; O-1 OPTION A RATIFIED BY PI DECISION:
  disposable-income operationalisation of the W2/W5 shift, manuscript
  disclosure mandated, Option B pre-registered as post-M08 robustness
  under deputy scope.
- **R-86** (2026-08-08). Stage-A independent review CLOSED (all items
  confirmed); O-3 adopted (c' >= 0 budget domain, dropped-alternative
  count reported); relocations moved to the commit card; contract v5
  = freeze candidate with the V6/V22/U6 ratification as the sole
  remaining freeze condition.

---

## Appended in v3 — R-87 … R-110 (2026-08-08 / 2026-08-23)

*Frame note, so the inconsistency is again declared rather than silent:
everything above this heading is **v2's full text, byte-identical** (sha256
`670de8adf5f23e6714195bacdb13258f1014d7d79f8aeb4f42717dd4e986e157`), copied by
file transfer under standing rule 8b and **not retyped or edited** — including
its title line and its v2 appendix heading, whose ranges and version markers are
therefore **stale on their face and are superseded by this appendix**. The
register this document carries now runs **R-59 … R-110**. The class, the
authorship (Goal-1 Manager governance record, NOT deputy text), the precedence
rule (*"Where this summary and a binding deputy instrument differ, the deputy
instrument controls"*), and the closing discipline paragraph all apply unchanged
to the entries below.*

- R-87 (2026-08-08). Stage-A final ratifications: V6/V22 materiality
  min{0.05, 0.5*(1-f_baseline)} per coalition, disjunctive halt with
  the 0.5-nat V_dir cross-check; U6 median adjacent-step drift <=
  0.05 nats; welfare_stage1_w3.yaml:32 VOID; S-10 spec digest
  acbf1637... adopted; U3 CLOSED; execution block lifted at freeze.
- R-88 (2026-08-08). Stage-A freeze closed at JMP HEAD 30d63f93;
  Stage-D split into implementation+battery gate and single headline
  run; U6 branch decision deferred to evidence.
- R-89 (2026-08-17). 1x V6/V22 halt VALID (f 0.7756; widening
  {A,B}/{B,P}/{A,B,P}); U6 Branch A ruled; promotion rule (smallest
  qualifying m, deputy return on NONE) and cross-check
  operationalisation (0.5-nat median, pilot-sized R) pre-registered;
  A-bar ratified ((dgn,educ3), IV-rank lower median, own support,
  maximal_opportunity excluded); three implementation bindings
  ratified; zero-operand halt guard permanent.
- R-90 (2026-08-17). AC-27 promotion outcome NONE (drift fails both
  m; persistent baseline disagreement; per-coalition cross-check
  structurally unevaluable - no g-hat sampler); deputy return
  declared; level-cancellation hypothesis recorded for deputy only.
- R-91 (2026-08-18). Deputy U6 proposal-remediation ruling adopted:
  proposal-axis repair, q^W defensive mixture, five managed stages,
  eight return conditions.
- R-92 (2026-08-18). q^W design FROZEN (Dq1-Dq10): node-level
  mixture lambda=0.25; r_E Bernoulli(0.5), r_Occ Uniform(4), r_H
  Uniform, r_W log-uniform (location-free Prop-1 bound); global box;
  deterministic t=0 mod 4 allocation, balance-heuristic MIS; atom
  Rao-Blackwellisation adopted; O-8 stratification pre-registered
  conditional; O-9 decile pilot rule; O-10 normalisation discriminator
  armed as halt; deputy path controls the design-memo location.
- R-93 (2026-08-18). O-10 fired pre-EUROMOD: g-tilde is an
  unnormalised household-specific kernel (log Z median +1.81, IQR
  0.42; hours/occupation/employment factors unnormalised; wage
  normalised); deputy return with normalisation remedy recommended.
- R-94 (2026-08-18). Deputy normalisation+notebook ruling adopted:
  g-hat = g-tilde/Z_i^S, analytic Z over full mixed support, N1-N9
  gate, old evidence reinterpreted; three verify-not-assume
  expectations pre-registered; Z's S-10 invariance an assertion.
- R-95 (2026-08-18). AC-30 RESUME-QW: N1-N9 clean (N4 relative, N8
  1e-12 - readings ratified); normalised drift table of record
  (0.119/0.107 then 0.087/0.076) supersedes the de-centred reading;
  alts==101 skip guard removed as hygiene; restricted log-Z table
  regenerable, never committed.
- R-96 (2026-08-18). AC-31 accepted (battery 7/7, bitwise reuse,
  ladder ~0.2h); O-8 stratified direct estimator ACTIVATED on its
  trigger (16.5h > 6h guard); welfare-side q_H corrected to the
  exact D1 mixture marginal (in-axis exactness); estimation-axis
  absorption expectation pre-registered with verify-or-escalate.
- R-97 (2026-08-19). Absorption REFUTED exactly (delta a
  (piece x component) function; F35 reference breaks span; |dLL| to
  1.47) - pre-registered deputy escalation; both-conventions framing;
  disclosure-only recommended; R<=583 fits the 6h guard under O-8.
- R-98 (2026-08-19). Deputy convention ruling adopted: disclosure-only
  REJECTED; P2a estimate suspended as parameter source; JMP-M08E
  commissioned (E0 audit -> successor input -> recovery -> re-
  estimation -> curvature/inference -> two reviews -> corrected
  handoff); q^W and all pricing held; notebook v3 delayed.
- R-99 (2026-08-19). E0-CONFIRMED-MARGINAL accepted (joint convention
  = labeled-space objective with m(h)=2 duplication; toy-choice
  decisive); ruling-file disk placement made a hard precondition;
  no-read-across pre-registration.
- R-100 (2026-08-19). AC-2 accepted (3 columns x 127,961 rows;
  NumPy/JAX 1e-13; recovery 30/30 with falsification control);
  successor spec identifier p2a_singles2016_region_live_margqh_v1
  registered; hours_mixture_d1 docstring fix deferred post-M08.
- R-101 (2026-08-19). E3 four-leg uniqueness standard replacing the
  voided 1e-5 coordinate criterion (error log 11); E3 reclassified
  CONVERGED-UNIQUE; theta resolution 7.5e-05 carried as disclosure.
  [Amended by R-103.]
- R-102 (2026-08-19). E4-PASS accepted; S-10 numbers mechanical
  re-derivation ratified; W-4 membership change routed to deputy;
  labelled-analogue gates assigned to Codex verification.
- R-103 (2026-08-19). Codex review REJECT accepted (T3 ACCEPT; T4
  scientific + 5 impl); rule-3 conversion invoked; R-101(d) AMENDED:
  minimal-observed-point selection mandatory, resolution/ULP waivers
  abolished (error log 12).
- R-104 (2026-08-19). AC-5C accepted: v2 successor artifacts (schema-
  faithful), fail-closed pin gate, tightened E3 completion (ten
  points, band 9.3e-10, minimal point selected, theta-bytes
  2cf320c3aa4b...), E4 v2 14/14 with T-12S/T-23S; narrow
  re-verification released, REJECT = E2.
- R-105 (2026-08-23). Re-verification R1/R5 REJECT -> E2 declared
  (error log 13); scientific chain fully accepted (R3 confirmed
  CONVERGED-UNIQUE); downstream held.
- R-106 (2026-08-23). Deputy E2-closure/notebook/RUM ruling adopted:
  bounded R1/R5 correction authorized; working notebook v3
  commissioned; RUM benchmark estimand design commissioned;
  CORRECTED S-10 SET {beta_l0_sm, beta_l0_sf, beta_l_nkids_sf} with
  FIVE scenarios, beta_w_pexp2 removed.
- R-107 (2026-08-23). AC-6C accepted (resolve() deleted with
  RouteRemoved; per-call re-hash; gate-state enforcement; registry
  52 keys; six probes RAISED; five-attempt inventory corrected);
  final verification released.
- R-108 (2026-08-23). Final verification R1-REJECT on an out-of-
  ruling condition (manager-transcribed overclaim; error log 14);
  R5 closed; deputy return with scope finding and micro-correction
  recommendation.
- R-109 (2026-08-23). Deputy disposition (b) adopted: R1/R5 CLOSED
  on the ruling's own terms; documentation-only correction with
  deputy replacement text; CORRECTED MARGINAL/MIS ESTIMATE ACCEPTED
  AS CURRENT TECHNICAL BASELINE; scientific-halt return list.
- R-110 (2026-08-23). AC-7a accepted (comment-only, AST-verified,
  module bcf0598d...); econometric review ACCEPT-WITH-REQUIRED-
  DISCLOSURES accepted - eight disclosures binding, A4 prohibited-
  claims standing; handoff v2 + rulings v3 + two gated commits
  authorized; baseline manuscript-facing.

---

## Appended in v4 — R-111 … R-132

*Frame note, so the inconsistency is again declared rather than silent:
everything above this heading is **v3's full text, byte-identical** (sha256
`094200a86aa23207d55266c52c36d617d6b16ca2171b637870b46474b199551b`), copied by
file transfer under standing rule 8b and **not retyped or edited** — including
its title line and its v2/v3 appendix headings, whose ranges and version
markers are therefore **stale on their face and are superseded by this
appendix**. The register this document carries now runs **R-59 … R-132**. The
class, the authorship (Goal-1 Manager governance record, NOT deputy text), the
precedence rule (*"Where this summary and a binding deputy instrument differ,
the deputy instrument controls"*), and the closing discipline paragraph all
apply unchanged to the entries below. Dates were not supplied with the R-111 …
R-131 log entries as received; none are fabricated here.*

- R-111. AC-9 freeze accepted; S-10 five-scenario table frozen; payloads
  local-evidence; .gitattributes queued.
- R-112. PKG-01A notice: env manifest requirement; standing rule 10.
- R-113. Env manifest accepted; optax observed-state; practice 11 explicit
  interpreter.
- R-114. u6resume3 accepted; layered registry + parity-discharge ratified;
  I_H theta-bearing; Z 4->2 collapse disclosure; V6/V22 crossing diagnostic.
- R-115. U6-C conversion bounded; practice 12 no hash().
- R-116. T4 frame corrected (err 15); four-item standard.
- R-117. NB3 label update + practices propagated.
- R-118. T4/U6-C closed by ruling (err 16; practice 13).
- R-119. Notebook v3-working accepted; e4v2 pins queued.
- R-120. Discussion notebook authorized.
- R-121. RUM-BENCH frozen: B1 RUM-B headline + RUM-A companion; B2-B6 as
  recommended; B7->M07I.
- R-122. R-118 split resolved (ddof mechanism; err 17).
- R-123. HK-01 ruling adopted; PKG-01A log line: "PKG-01A closed under Goal 2
  on branch pkg/pk01a-notebook-api-stabilisation; code close
  b82d03de82bc505e4b0d12ce83daccbc5e1749a0, branch tip with acceptance record
  301e54678ced97a0dc1d081cf07a7934f98ea077; package main, MNL's pinned
  package revision, and the MNL gitlink remain unchanged."
- R-124. NB3-AC-2 + HK1 phase-1 provisional; delta mandated; scope rulings.
- R-125. F35 finding three-way classification; NB3-AC-4 scope.
- R-126. U6-D NONE -> deputy; 1/sqrt(m) reading; loc4 disclosure queued; RUM
  shut-off refused.
- R-127. MC-precision ruling adopted; GAMS Taylor defect -> PKG backlog;
  run-B banked.
- R-128. AC-39 STOP valid (W2 anchor defect); RUM-AC-2 accepted; B5 scopes
  ratified; nesting finding recorded.
- R-129. Welfare-measure disposition adopted (W1/W4/W6 roles; W2
  compatibility finding; RUM fence; leg-(c) class rule). Full deputy text
  embedded verbatim below under "Deputy Ruling — Welfare-Measure
  Disposition, 8x Extension, and RUM Interpretation".
- R-130. 8x FAIL(named) -> deputy; box-escape structural; c_scale local fix.
- R-131. AC-41 ACCEPT-WITH-DISCLOSURES + provenance completion; 16x ruling
  adopted (revised contribution rule, S-10 CRN differences, box freeze,
  F_star panel). Full deputy text embedded verbatim below under "Deputy
  Ruling — Final 16x Extension and Scale-Consistent Normalized-Contribution
  Precision".
- R-132 (2026-08-25). JMP-HK-01 Phase-1 execution register ratified: rows
  265/322 reclassified HOLD -> HISTORICAL_IMMUTABLE_IN_PLACE; external
  archive root designated C:\Users\hisham\JMP_Archives\HK01\2026-08-25_
  ratified_v1\; v4 execution register created from v3 (parent sha256
  30d0999e...), mechanical gate ALL_PASS (14/14). Phase 2 remains blocked
  pending U6-E review, U6 evidence commit, and no-live-target conditions.
  Full deputy text embedded verbatim below under "Deputy Ruling — JMP-HK-01
  Phase-1 Execution Register Ratification".

No welfare number, decomposition number, parameter value, or estimation is
produced or implied by the entries above. This document records; it does not
newly rule.

---

## Deputy Ruling — Welfare-Measure Disposition, 8x Extension, and RUM Interpretation (R-129)

*Recorded per its §9 no-separate-file instruction. Embedded verbatim, as
pasted, with no editing.*

DEPUTY RULING — WELFARE-MEASURE DISPOSITION, 8x EXTENSION,
AND RUM INTERPRETATION

1. W2

Do not alter W2 to a high quantile, smooth maximum or soft maximum.

The characterised W2 requires the maximum pay over the actual ability
set after a common income shift. The current implementation uses a
maximum over sampled disposable-income nodes. Its divergence with draw
count is therefore definitional, not ordinary Monte Carlo noise.

W2 is not operational under the current continuous latent-job support
using the sampled-set maximum.

Remove W2 from:
- the M08 source decomposition;
- the S-10 headline functional;
- paper-facing quantitative welfare inequality.

Retain it as a theory-empirics compatibility finding. A bounded-support
or quantile analogue would be a new measure with a new name and is
deferred for discussion with François.


2. REVISED M08 MEASURE ROLES

Primary decomposition:
W1.

Preferred secondary full-compensation decomposition:
W4.

Pre-registered fallback secondary if W4 fails and W6 passes:
W6.

W3:
validation only.

W5:
signed welfare-distribution diagnostic only. Report weighted mean,
median, quantiles, minimum, maximum and weighted/unweighted counts
below, at and above zero. Do not report a conventional Gini or run a
Gini-Shapley decomposition on W5. Apply no shift, floor or absolute
transformation.

W2:
not operational in M08.

For W1 report:
phi_A, phi_B, phi_P, R_bg and
s_opp = (phi_A + phi_B) / I(empty).

For W4 or fallback W6 report the same secondary decomposition.


3. AUTHORISE ONE FINAL NESTED 8x BASIS

Use the unchanged qW, normalisation, target-only pricing path, model,
theta, operators and references.

Create an 8x basis under the existing nested ladder. No proposal
redesign.

Freeze eight disjoint 1x-equivalent stochastic blocks D1...D8 and four
adjacent 2x superblocks:

B1 = D1 union D2
B2 = D3 union D4
B3 = D5 union D6
B4 = D7 union D8

For every paper-facing functional compute:

- full 8x T8;
- all six 4x pair-of-superblock estimates T4_ab;
- four 6x leave-one-superblock-out estimates T6_-b.

Define:

T4_bar = mean(T4_ab)

T6_bar = mean(T6_-b)

SE_MC(T8)
= sqrt[(3/4) * sum_b (T6_-b - T6_bar)^2]

E_T = max(
    abs(T8 - T4_bar),
    1.96 * SE_MC(T8)
)

Use the existing thresholds:

mean/median:
E_T / S_k <= 0.0025

Gini/headline inequality:
E_T <= 0.00125

decomposition shares:
E_T <= 0.005

sign and ordering:
identical between 8x and every 6x leave-one-block-out estimate.

Before any Gini, report support minimum, weighted/unweighted
non-positive counts, weighted mean and proximity to zero. No automatic
shift or floor.

Promote M08 only if:

- all hard qW, normalisation, direct-agreement, operator and accounting
  gates remain passed;
- W1 passes all level, Gini and decomposition thresholds;
- at least one of W4 or W6 passes all level, Gini and secondary-
  decomposition thresholds;
- every reported inequality index satisfies its support domain;
- the independent U6 review accepts.

W4 has priority over W6 if both pass.

W5 and W2 do not block promotion under their revised roles.

If W1 fails, or both W4 and W6 fail, halt and return. Do not
self-authorize more ordinary draws, QMC, control variates or another
gate change.

Reuse existing direct-redraw evidence where possible and recompute
direct-vs-IS agreement against the 8x IS values. No new direct-redraw
pricing unless the frozen flagged set changes mechanically.


4. S-10

Keep the five corrected scenarios:

baseline;
beta_l0_sm alone;
beta_l0_sf alone;
beta_l_nkids_sf alone;
all three jointly.

Anchor the formal Tier-2 opportunity-share trigger on W1:

s_opp_W1 = (phi_A_W1 + phi_B_W1) / I_W1(empty).

Report the sensitivity for the accepted W4/W6 secondary comparator,
but the formal trigger is W1. Existing materiality thresholds remain
unchanged.


5. RUM-B

Accept RUM-B as the technically preferred common-opportunity benchmark,
subject to the planned independent econometric review.

Before manuscript use, produce an exact nesting map:

- five RUM parameters;
- five RURO terms they replicate;
- algebraic mapping;
- included opportunity channels;
- excluded regional, occupation, wage or interaction channels.

Do not state globally that the likelihood cannot distinguish
opportunities from tastes unless the map proves that scope.

The permitted interpretation is observational equivalence within the
tested block. Do not frame the LR result as model selection, causal
identification or proof that RURO is empirically preferred.

The potential paper contribution is that observationally similar
representations can produce different welfare attribution.


6. RUM-A LEG (c)

Apply the barrier criterion only across materially distinct convergence
classes.

Points inside the already frozen objective-collapse/optimizer-
termination band, with the same active set and the same accepted
first-order status, are one numerical optimum.

An internal segment is recorded as:

NOT_APPLICABLE_SAME_CONVERGENCE_CLASS

It is not a waiver.

Given the one-ulp discrepancy on a segment within the termination band
and clean rays to all five materially separated starts, RUM-A leg (c)
is satisfied by reclassification. No new optimization is required.

RUM-A remains supplementary; RUM-B is headline.


7. LOC4 AUDIT

Accept the loc4 -2 to 4 audit as clean.

Retain and disclose the deterministic seven-row chosen-alternative
mode imputation in:
- notebook data cleaning;
- data appendix;
- specification-limits block.

No remapping, dropping, repricing or re-estimation.


8. WORKING NOTEBOOK

Update the working v3 notebook now with:

- corrected RURO results;
- RUM-A and RUM-B results;
- LR and nesting map;
- disposable-income inequality;
- qW validity diagnostics;
- all-coalition direct-vs-IS diagnostics;
- provisional 4x W1/W4/W6 results labelled
  PROVISIONAL_NOT_PROMOTED_MC_PRECISION;
- W5 sign-support diagnostics;
- W2 sampled-maximum divergence;
- the loc4 disclosure;
- the remaining 8x/S-10/LOC4 roadmap.

Add a boxed theory-empirics discussion item explaining that replacing
the W2 maximum with a quantile or smooth maximum would define a new
measure.

Do not present any 4x welfare or decomposition magnitude as accepted.


9. PROCESS

Record this ruling in the existing consolidated M08 rulings document.
Do not create a separate deputy ruling file.

Issue complete NEXT ACTION CARDS for:

A. 8x nested generation/pricing and functional evaluation;
B. independent GPT-5.6 Thinking U6 review;
C. independent RUM econometric/results review;
D. working-notebook update.

Every card must state tool, model, thinking, effort, repository,
attachments/paths, exact prompt, outputs, prohibitions, return and
halt conditions.

Return to the deputy only if:

- W1 fails at 8x;
- both W4 and W6 fail at 8x;
- a measure's support invalidates the intended index with no frozen
  alternative;
- the independent RUM review rejects the nesting interpretation;
- a Tier-2 S-10 trigger fires;
- a package change is required;
- disclosure fails;
- or LOC4 sequencing conflicts.

---

## Deputy Ruling — Final 16x Extension and Scale-Consistent Normalized-Contribution Precision (R-131)

*Recorded per its §9 no-separate-file instruction. Embedded verbatim, as
pasted, with no editing.*

DEPUTY RULING — FINAL 16x EXTENSION AND SCALE-CONSISTENT
NORMALIZED-CONTRIBUTION PRECISION

1. 8x STATUS

Do not promote 8x.

Signs and component ordering are qualitatively stable, but the primary
W1 mean/median/Gini functionals have not passed the frozen precision
standard.

2. COMPONENT LEVELS AND NORMALIZED CONTRIBUTIONS

Keep the absolute Gini-scale gate for:

phi_A
phi_B
phi_P
R_bg
phi_A + phi_B

Require:

E_T <= 0.00125

For a normalized signed contribution:

r_T = T / I(empty)

define:

S_r = max(1, abs(r_T))

and require:

E_r / S_r <= 0.005

Also require:

abs(I(empty)) > 20 * E_I(empty)

When a normalized contribution lies outside [0,1], label it:

SIGNED NORMALIZED CONTRIBUTION RELATIVE TO BASELINE WELFARE
INEQUALITY

Do not present it as a conventional non-negative share, and do not use
pie charts.

This replaces only the mis-scaled absolute gate on the LEVEL of the
ratio.

3. S-10 REMAINS ABSOLUTE

For every S-10 scenario compute directly, under common random numbers:

Delta_s_opp_r = s_opp_r - s_opp_baseline

Require:

E_Delta_s_opp_r <= 0.005

The Tier-2 trigger remains:

abs(Delta_s_opp_r) >= 0.02

If the MC interval overlaps 0.02, classify:

S10_MATERIALITY_INDETERMINATE_MC

and return.

Do not infer non-trigger status from the point estimate alone.

4. AUTHORISE ONE FINAL 16x BASIS

Create 16 disjoint 1x-equivalent stochastic blocks D1...D16.

D1...D8 must remain the bitwise existing 8x prefix.

Freeze four 4x superblocks:

B1 = D1 union D2 union D3 union D4
B2 = D5 union D6 union D7 union D8
B3 = D9 union D10 union D11 union D12
B4 = D13 union D14 union D15 union D16

For every paper-facing functional compute:

- full 16x T16;
- six 8x pair-of-superblock estimates T8_ab;
- four 12x leave-one-superblock-out estimates T12_-b.

Define:

T8_bar = mean(T8_ab)

T12_bar = mean(T12_-b)

SE_MC(T16)
= sqrt[(3/4) * sum_b (T12_-b - T12_bar)^2]

E_T = max(
    abs(T16 - T8_bar),
    1.96 * SE_MC(T16)
)

Retain:

mean/median:
E_T / S_k <= 0.0025

Gini:
E_T <= 0.00125

component levels:
E_T <= 0.00125

sign and ordering:
identical between T16 and every T12_-b estimate.

No ordinary basis beyond 16x is authorised.

5. DEFENSIVE SUPPORT BOX

Ratify the 8x support-box rebind as a one-time correction because the
prior limits were realised ranges, not structural design bounds.

Before generating D9...D16:

- record and hash the current rebound box;
- freeze it permanently for the 16x run;
- do not rebind after observing 16x draws;
- do not regenerate or reprice D1...D8.

The defensive component density r is zero outside its box.

For an ordinary-q draw outside the box, evaluate exactly:

qW = (1-lambda) * q

Do not halt and do not expand the box.

If the implementation cannot apply this piecewise density exactly,
halt before EUROMOD and return.

6. c_scale

Fix stage_basis prospectively so every rung computes and stores its own
c_scale.

Do not overwrite historical accepted sidecars.

Create rung-specific successor sidecars in the 16x attempt.

Using existing 1x/2x/4x/8x evidence, verify that replacing the stale
value with the rung-specific value leaves every claimed scale-invariant
functional unchanged within 1e-12.

If any changes beyond 1e-12, halt.

7. FIXED DIRECT-VALIDATION PANEL

Do not use the collapsing current-ESS flagged set as the sole final
gate.

Before 16x evaluation define and hash:

F_star = union across all coalitions of their 4x ESS-flagged
households.

For every coalition evaluate direct-vs-IS on every household in
F_star.

Reuse existing direct prices. Price only missing coalition-household
combinations, subject to the existing six-hour guard.

If projected incremental cost exceeds six hours, return before
execution.

Gate per coalition:

median over F_star of abs(V_dir - V_IS_16x) <= 0.5 nats

Report panel n, p90, max, share >0.5, signed median/IQR, and the
current 16x flagged-set diagnostic separately.

If F_star has fewer than five distinct households, return before using
it as certification evidence.

8. PROMOTION

Promote 16x only if:

- all hard normalisation, qW, tail, operator, reference and disclosure
  gates remain passed;
- support-box and c_scale corrections pass;
- the fixed-panel direct check passes for all eight coalitions;
- W1 mean/median/Gini pass;
- W1 component levels pass;
- W1 normalized contributions pass the revised rule;
- at least one of W4 or W6 passes the corresponding secondary gates;
- all sign/order checks pass;
- direct S-10 difference precision passes and Tier-2 does not fire;
- Shapley/fixed-background accounting passes;
- the independent U6 review accepts.

W4 has priority over W6 if both pass.

W2 remains non-operational.
W5 remains signed-distribution diagnostic only.

If W1 fails, both W4/W6 fail, the fixed-panel direct check fails, or
S-10 is indeterminate, return.

Do not self-authorize 32x, QMC, control variates, another proposal
redesign, or another gate change.

9. REVIEW AND PROCESS

Before EUROMOD, commission one bounded GPT-5.6 Codex read-only review
of only:

- frozen-box hash and piecewise r=0 outside-box density;
- exact qW evaluation;
- rung-specific c_scale;
- nested 8x-prefix/16x construction;
- fixed F_star construction.

One narrow correction cycle is permitted.

Then execute the 16x extension and commission the existing independent
GPT-5.6 Thinking U6 numerical/economics review.

The RUM independent review continues in parallel.

Record this ruling in the existing consolidated M08 rulings document.
Create no separate deputy ruling file.

Return only under the promotion failures listed above, a valid Codex
rejection, a Tier-2 trigger, package change, disclosure failure, or
LOC4 conflict.

---

## Deputy Ruling — JMP-HK-01 Phase-1 Execution Register Ratification (R-132)

*Recorded per its §3/§9 no-separate-memo instruction. Embedded verbatim, as
pasted, with no editing.*

DEPUTY RATIFICATION — JMP-HK-01 PHASE 1

The Phase-1 review chain is accepted. The reviewed v3 register remains
the evidence anchor, but create a v4 execution register with exactly
two deputy amendments.

1. ROWS 265 AND 322

Reclassify both from:

HOLD

to:

HISTORICAL_IMMUTABLE_IN_PLACE

A current review's exact-path citation is sufficient to establish an
active evidentiary-lineage predicate.

For both rows:

- retain the current path;
- clear every archive/move/delete action and archive-plan field;
- use this reason:

"Exact-path citation by a current review establishes an active
evidentiary-lineage dependency; the historical predecessor must
remain immutable in place."

Revised counts:

CANONICAL_CURRENT: 937
HISTORICAL_IMMUTABLE_IN_PLACE: 127
ARCHIVE_MOVABLE: 13
DELETE_TRACKED: 0
ARCHIVE_EXTERNAL_THEN_DELETE_UNTRACKED: 48
HOLD: 8262
TOTAL: 9387

2. EXTERNAL ARCHIVE ROOT

The designated private local archive root is:

C:\Users\hisham\JMP_Archives\HK01\2026-08-25_ratified_v1\

It is outside all active repositories and worktrees.

Preserve source-tree identity and the full relative path below the
root, using top-level folders such as:

MNL\
Job_Market_paper\
MNL_external_outputs\

Do not flatten filenames.

Create the archive root only when Phase 2 is authorized to begin.

3. V4 EXECUTION REGISTER

Create:

docs/Missions/HK01/
JMP_HK_01_inventory_and_disposition_register_v4.csv

Preserve v3 unchanged as reviewed history.

V4 may differ from v3 only by:

- the two row-265/322 class changes;
- their reason/no-action field corrections;
- replacement of the archive-root placeholder on the 48 untracked
  archive candidates;
- mechanically revised class-count metadata, if present.

No other row, class, source hash, protection field, action or path may
change.

Compute and record the v4 SHA-256.

4. MECHANICAL V4 GATE

Verify before execution authority:

- 9,387 rows and 24 columns in both v3 and v4;
- unchanged row order and stable IDs;
- only rows 265, 322 and the 48 archive-path fields differ;
- rows 265/322 contain no action or archive-plan residue;
- all 48 paths are strictly below the designated archive root;
- no CANONICAL_CURRENT, HISTORICAL_IMMUTABLE_IN_PLACE or HOLD row has
  an executable move/delete action;
- pin, manifest, pointer, current-reference and ruling-protection
  populations are unchanged;
- counts equal 937 / 127 / 13 / 0 / 48 / 8262;
- the reviewed v3 SHA is recorded as the parent register.

No new broad Codex review is required.

Record this ruling in the existing consolidated Goal-1 rulings
document. Do not create a separate deputy-ratification memo.

5. PHASE-2 AUTHORITY

Phase 2 remains blocked until:

- U6-E independent review is complete;
- the U6 results/evidence commit is complete;
- no live review, notebook run, pricing process or manuscript task
  targets an affected path;
- v4 passes the mechanical gate.

Once all four hold, Phase 2 may proceed autonomously.

The executable scope is only:

- 13 ARCHIVE_MOVABLE rows;
- 48 ARCHIVE_EXTERNAL_THEN_DELETE_UNTRACKED rows.

Do not touch the other 9,326 rows.

6. UNTRACKED ARCHIVE/DELETE RULE

For every untracked row:

- hash the source;
- copy to the exact ratified archive path;
- hash the archive copy;
- verify byte identity and size;
- write the archive-manifest row;
- only then delete the source;
- verify source absence and record deletion time.

If a row is raw microdata, EUROMOD system/input material, an
undisclosure-reviewed household-level artifact, or an active input,
reclassify it to HOLD and halt that row.

No wildcard deletion and no git clean command.

7. TRACKED ARCHIVE RULE

For the 13 ARCHIVE_MOVABLE rows:

- use git mv;
- preserve relative lineage;
- update only ratified non-pinned references;
- rerun the complete reference scan;
- use separate gated commits in MNL and Job_Market_paper.

No tracked deletion is authorized because DELETE_TRACKED count is zero.

Return to the deputy only if:

- the v4 mechanical gate fails;
- a Phase-2 condition cannot be established;
- a protected/pinned/current artifact would need to move;
- an untracked file cannot be archived and verified;
- the ratified register requires substantive revision;
- or an existing U6 scientific halt fires.

### Execution note (mechanical, not a new ruling)

v4 created 2026-08-25 from v3 (parent sha256
`30d0999e56e29a6ef513359b0a708512f1780d4b63f76cc22d5aba23a16f2937`). v4
sha256: `4cb3bda72d26f5b723756a85b31ab617361e18f6e467b7cf00855073c9a61126`.
Mechanical gate: 14/14 PASS (see execution register commit / assistant
report for full table and diff line list). Only rows 265, 322 (class
reclass) and the 48 ARCHIVE_EXTERNAL_THEN_DELETE_UNTRACKED archive_path
fields differ from v3. No Phase-2 action taken — archive root not
created, no files moved or deleted, no git mv/git rm issued. Phase 2
remains blocked per §5 above until its four conditions are independently
established.

---

## Appended 2026-08-25 — R-133 … R-140

*Frame note: chat-side rulings R-132..R-139 renumbered on-disk R-133..R-140;
the HK-ratification adoption in R-137's content cross-references on-disk
R-132.*

- R-133 HK1-AC-3 register v2 accepted; rulings v4 built (prefix
  byte-identical to v3).
- R-134 AC-43 16x prebuild accepted; normalized-contribution
  threshold transmission corrected (S_r rule verbatim); HK1-AC-5/6
  released.
- R-135 AC-41 completion (ACCEPT-WITH-DISCLOSURES; four amended
  wordings binding; RUM-A label supersession required); HK
  re-review NOT-RATIFIABLE -> coherence correction; AC-44 R2
  correction cycle invoked.
- R-136 Preflight R2 corrected and PASS overall; HK Phase-1
  ratification adopted (on-disk R-132); validation register
  (synthetic recovery / holdout / cross-year) recorded as packet
  proposal; 16x execution released.
- R-137 HK1-AC-7 v4 register gate 14/14; archive-path redundancy
  accepted as ratified literal substitution; Codex-as-implementer
  routing option adopted with per-cycle independence.
- R-138 AC-47 16x FAIL(named) returned; NB3-AC-7 discussion-notebook
  update + HTML accepted; deputy U6-CV1 ruling adopted (option c
  with ceiling; median MC_BAND_ONLY_NONSMOOTH; limited-precision
  fallback pre-authorized).
- R-139 U6-CV1 DESIGN FREEZE: Dcv1-Dcv10 frozen per JMP_M08_U6_CV1_
  control_variate_design_v1.md; O-1 resolved single-box on the
  8x-rebind/bitwise-prefix/zero-off-box record (Stage-A
  verification mandatory); O-4/O-5/O-6 ratified; O-7 constants
  kappa=2.0, (a,b,d)=(1.0,0.5,-0.25), a-degree=0.5, |t-degree|<=
  3.0; O-10 degenerate-fold share threshold 1%; ruling s8
  promotion conjunction transcribed: W1 mean+Gini pass; all W1
  component levels pass; all primary normalized contributions
  pass; denominator gate; signs/order invariant; S-10 precise,
  Tier-2 not fired; one of W4/W6 full pass (W4 priority); support
  gates; independent review accepts; median not a gate.

---

## Deputy Ruling — U6-CV1 and Limited-Precision Fallback (R-138)

*Recorded per its §10 no-separate-memo instruction. Embedded verbatim, as
pasted, with no editing.*

DEPUTY RULING — U6-CV1 AND LIMITED-PRECISION FALLBACK

1. RAW 16x STATUS

Do not fully promote raw 16x.

Treat as decision-grade under the returned evidence:

- W1 mean;
- W1 Gini;
- phi_A;
- phi_B;
- component signs;
- ordering B > A > P;
- direct S-10 scenario differences and the non-trigger verdict.

Treat as MC-banded, not fully precise:

- W1 median;
- phi_P;
- R_bg;
- phi_A + phi_B;
- s_opp;
- remaining normalized contributions;
- W4/W6 quantitative comparisons.


2. MEDIAN

Remove the absolute baseline median from the M08 promotion gate.

Report it with:

MC_BAND_ONLY_NONSMOOTH

including the 16x estimate, block estimates and E_T.

For S-10, continue to gate the common-random-number median DIFFERENCE:

- interval fully inside +/-1% => no trigger;
- interval fully outside => Tier 2;
- interval crossing the threshold =>
  S10_MEDIAN_MATERIALITY_INDETERMINATE_MC
  and return.


3. AUTHORISE ONE ZERO-EUROMOD CONTROL-VARIATE ATTEMPT

Mission stage:

JMP-M08 U6-CV1 — Cross-Fitted Control-Variate Closure

Use only the already priced 16x basis.

No new EUROMOD.
No new draws.
No theta, qW, normalisation, reference, coalition, S-10, LOC4 or
package-source change.

The control variate acts on each IS integral before log and inversion.


4. FIXED CONTROL LIBRARY

Before any CV-adjusted welfare result, freeze a common library using
only controls with exact analytic expectations under qW:

- centered proposal-component indicators;
- centered hours-band indicators;
- centered occupation-category indicators;
- centered first log-wage moment;
- centered second log-wage moment.

Omit one category per complete indicator family.

No interactions.
No polynomial order above two.
No feature selection based on failed functionals.
No post-result tuning.
The non-employment atom remains Rao-Blackwellized.

If an exact expectation cannot be proved for a control, omit it before
execution.


5. CROSS-FITTING

Use the four frozen 4x superblocks.

For every household, coalition, scenario and attained/reference
integral:

- hold out one 4x block;
- estimate coefficients on the other 12x nodes;
- evaluate on the held-out block;
- repeat four times;
- average the four held-out integral estimates;
- then apply log, inversion and welfare construction.

The evaluation block must never estimate its own coefficient.

Freeze any conditioning/ridge convention before results. No
result-driven penalty choice.


6. CV INTEGRITY

Require:

- analytic control expectations within 1e-12;
- constant-integrand identity within 1e-12;
- zero coefficient reproduces raw estimator bitwise;
- every adjusted integral finite and strictly positive;
- same normalised target integral;
- block/CRN identities unchanged;
- Shapley accounting exact;
- F_star direct-vs-CV median <=0.5 for all eight coalitions;
- analytic synthetic examples show no bias.

Any non-positive integral or target mismatch rejects CV1.


7. GLOBAL ADOPTION RULE

No per-functional estimator switching.

Adopt the CV estimator globally only if:

- E_T weakly decreases for W1 mean, W1 Gini,
  phi_A+phi_B and s_opp;
- no W1 component-level E_T rises by more than 10%;
- all signs and ordering remain unchanged;
- at least one previously failing primary functional improves by 20%;
- every CV integrity gate passes.

Otherwise retain raw 16x.


8. FULL QUANTITATIVE PROMOTION IF CV SUCCEEDS

Require:

- W1 mean and Gini pass;
- all W1 component levels pass;
- all primary normalized contributions pass;
- denominator-strength gate passes;
- signs/order pass;
- S-10 differences pass and Tier 2 does not fire;
- at least one of W4 or W6 passes the corresponding secondary gates;
- support/domain gates pass;
- independent U6 review accepts.

Median is not a promotion gate.

W4 has priority if both W4 and W6 pass.


9. FALLBACK IF CV DOES NOT CLOSE

Do not open another numerical-instrument loop.

Route raw 16x to the independent U6 review under:

M08_BASELINE_PROTOTYPE_LIMITED_MC_PRECISION

Permitted quantitative headlines:

- W1 mean with MC band;
- W1 Gini with MC band;
- phi_A with MC band;
- phi_B with MC band.

Permitted qualitative claims:

- stable component signs;
- stable ordering B > A > P;
- opportunity-related variation is non-negligible;
- S-10 does not alter the qualitative conclusion.

Report all other decomposition magnitudes and s_opp as
estimate +/- E_T, not as exact headline shares.

Do not claim quantitative robustness across W4/W6 if neither passes.

Limited-precision acceptance closes the M08 prototype only.
Final quantitative magnitudes remain blocked until LOC4 and a final
precision decision on the preferred specification.


10. REVIEW AND ROUTING

Issue complete NEXT ACTION CARDS for:

A. CV design freeze:
   Claude Project Opus, thinking ON, high effort.

B. CV implementation and bounded review:
   Claude Code Opus, then GPT-5.6 Codex read-only.

C. CV/raw functional evaluation:
   existing priced 16x basis, zero EUROMOD.

D. independent U6 numerical/economics review:
   GPT-5.6 Thinking, high reasoning.

E. working-notebook update.

The independent reviewer must review both:

- the full-promotion route if CV succeeds;
- the limited-precision claim set if it does not.

Record this ruling in the consolidated M08 rulings document.
Create no separate deputy memo.

Return only if:

- CV changes the estimand;
- a CV integral is non-positive;
- direct-vs-CV fails;
- the independent review rejects both full promotion and the
  limited-precision fallback;
- S-10 becomes indeterminate or triggers Tier 2;
- LOC4 cannot proceed under the banded framework;
- a package change is required;
- or disclosure fails.
