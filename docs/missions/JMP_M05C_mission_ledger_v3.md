# JMP-M05C Mission Ledger v3

**Mission:** JMP-M05C — Minimal Streaming Inference Implementation
**Goal manager:** Goal 1 Manager — Empirical JMP (operating contract v1)
**Supersedes:** ledger v2 (committed history)
**Ledger updated:** 2026-08-03
**Target repository path:** `docs/missions/JMP_M05C_mission_ledger_v3.md`

## 1. Binding revisions

MNL **Increment-B HEAD `c2cf6a36a17e63f6da9922b2cf0fe0e8b9a1fd36`** (← Increment-A `92e299de…` ← addendum `b5169293…` ← closeout `ffd060f7…`). `Job_Market_paper` `7195fc50…` (instrument checkpoint queued). Nested = gitlink `27756a06…` (frozen). Bundles: Phase-3 `2cf23764…864b`, Phase-4 `5484…65f3` (separate manifest-excluded hash-of-hashes). Binding design: v4 + streaming addendum; **certification proportionality rule v1 in force (standing rule 7)** — blocking = scientific correctness, disclosure/no-row-persistence, accepted-artifact binding, production-path integrity; general software-security hardening non-blocking.

## 2. Increment tracker

| Stage | Status |
| --- | --- |
| Increment A | CLOSED at `92e299de…` (arc in ledger v2) |
| Increment B | **CLOSED at `c2cf6a36…`**: delivered → Review B v1 REJECT (6 fixes) → rule-3 conversion (spent) → refix (3 of 6 verified) → Review B v2 REJECT (3 residuals) → E2 → deputy proportionality decision (core ACCEPTED) → frozen-probe three-fix closure (B-1 non-overridable T-22 names; B-2 validate-before-write; B-3 `extra=` removed, close-by-construction) → focused verification **PASS** (live probe re-runs; frozen hash `5ae1aa88…` = committed test-file hash, freeze anchor closed) → ten-file commit incl. both proportionality records |
| Increment C — runner, transactions, reproduction | **OPEN** (card M05C-AC-16) |
| Review C → final integrated review → exact-state commit → ONE aggregate-only dry run → Goal-1 audit → deputy packet | PENDING |
| Production real run | deputy-reserved |

## 3. Increment C blocking review scope (deputy-fixed; pre-registered)

Actual production runner; fresh-process aggregate reproduction (T-12S at the frozen digest tuple); aggregate-only transaction; STOPPED truthfulness; no `complete/`; no row-level persistence (T-23S); accepted-artifact/revision binding; numerical regression. **Not blocking:** import-surface counting, capability tokens, general software-security certification (nonblocking-debt register at most).

## 4. Rulings and debt (delta since v2)

R-38 conversion (spent) · R-39 refix acceptance · R-40/R-41 second-REJECT halt + E2 · R-42 closure acceptance incl. close-by-construction endorsement; manager drafting defects #3 (heading conflict) and #4 (file-placement pre-step) owned — **all state dispositions and file placements now in-prompt, verified** · R-43 reviewer substitution (Sonnet for the focused verification; Codex reserved for Review C/final; vendor-independence caveat disclosed for the final packet) · R-44 focused PASS; freeze-timing observation closed by commit · R-45 commit accepted; executor's provenance-verified file recovery ratified. Debt register (nonblocking): `_record` dead check; in-memory metadata on write_matrix/table; foreclosed scalar extensions; pandas stubs; `assert_aggregate_payload` pairing.

## 5. Next authorized action

Card M05C-AC-16 — Increment C in the implementer session; Review C under the §3 scope (Codex when quota permits; R-43 pattern otherwise).
