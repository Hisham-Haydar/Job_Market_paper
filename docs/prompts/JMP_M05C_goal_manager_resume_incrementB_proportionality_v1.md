# Prompt — Resume JMP-M05C Under Increment-B Proportionality Decision

Use in the existing persistent:

`Goal 1 Manager — Empirical JMP`

**Model:** Fable; Opus fallback  
**Thinking:** On  
**Effort:** High

---

Adopt as binding:

- `JMP_certification_proportionality_rule_v1.md`
- `JMP_M05C_incrementB_proportionality_decision_v1.md`

The Increment-B numerical and econometric core is accepted. Do not commission
another broad remediation or architecture review.

Manage autonomously:

1. commission the finite three-fix closure using
   `JMP_M05C_incrementB_three_fix_closure_prompt_v1.md`;
2. review the closure report for exact scope;
3. commission the focused Codex verification using
   `JMP_M05C_incrementB_focused_closure_review_prompt_v1.md`;
4. if verdict is PASS:
   - commit the exact reviewed Increment-B source, tests, reports, reviews, and
     proportionality records;
   - require clean MNL and nested worktrees;
   - update the JMP-M05C ledger;
   - launch Increment C immediately;
5. if verdict is FAIL:
   - determine whether the failure is only a direct implementation error in one
     of the same three frozen probes;
   - if yes, authorize one mechanical correction and rerun the same focused
     review;
   - otherwise return to the deputy programme director.

For Increment C, enforce the certification proportionality rule. The blocking
review scope is:

- actual production runner;
- fresh-process aggregate reproduction;
- aggregate-only transaction;
- STOPPED truthfulness;
- no `complete/`;
- no row-level persistence;
- accepted-artifact/revision binding;
- numerical regression.

Do not reopen import-surface, capability-token, or general software-security
certification.

Every operational reply must contain a complete NEXT ACTION CARD. Choose the
tool, model, thinking, effort, workspace, files, exact prompt, expected outputs,
return destination, and halt conditions without asking Hisham.

Your first action card must use:

- Tool: Claude Code
- Model: Opus
- Thinking: On
- Effort: Medium-high
- Prompt: full contents of
  `JMP_M05C_incrementB_three_fix_closure_prompt_v1.md`
- Return destination: this Goal 1 Manager chat

Return to the deputy programme director only after:

- the final audited aggregate-only dry run; or
- a genuine scientific, disclosure, accepted-artifact, or production-path E2
  issue.

The production real run remains unauthorized.
