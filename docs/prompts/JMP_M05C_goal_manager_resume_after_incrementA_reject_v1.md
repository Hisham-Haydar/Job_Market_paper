# Prompt — Resume JMP-M05C After Increment-A Review REJECT

Use in the existing persistent:

`Goal 1 Manager — Empirical JMP`

**Model:** Fable; Opus fallback  
**Thinking:** On  
**Effort:** High / long-horizon management

---

Adopt `JMP_M05C_incrementA_E2_deputy_decision_v1.md` as binding.

The Increment-A review REJECT is valid. One bounded five-fix refix is
authorized. Increment B remains blocked.

Manage autonomously:

1. commission the refix in the same fresh Increment-A implementer session using
   `JMP_M05C_incrementA_bounded_refix_prompt_v1.md`;
2. review the returned refix report for exact five-fix scope and state;
3. commission a fresh independent Codex Review A v2 using
   `JMP_M05C_incrementA_review_v2_prompt_v1.md`;
4. if Review A v2 returns APPROVE:
   - commit the exact reviewed Increment-A source, tests, report, refix report,
     and Review A v2;
   - require clean MNL and nested worktrees;
   - update the JMP-M05C mission ledger;
   - launch Increment B;
5. if Review A v2 returns REJECT:
   - halt immediately;
   - preserve state;
   - return to the deputy programme director;
   - do not commission another Increment-A fix.

Apply the mission-wide process amendment:

- every state deletion/move/cleanup is performed inside a gated task prompt;
- every full-suite command explicitly deselects test 29 unless test 29 is the
  isolated target with a temporary output and cleanup contract;
- for Increments B and C only, you may convert one implementation-only REJECT
  into one bounded refix without deputy return when every condition in deputy
  decision §3 is satisfied;
- a second REJECT for the same increment, or any design/package/architecture
  issue, is an immediate E2 halt.

Update `JMP_M05C_mission_ledger_v1.md` with:

- Increment-A Review v1 REJECT;
- deputy-authorized bounded refix;
- exact five-fix list;
- state-disposition rule;
- test-29 deselection rule;
- prospective B/C review-conversion amendment;
- Increment B blocked pending Review A v2 APPROVE.

Every operational reply must contain a complete NEXT ACTION CARD.

Your first action card must specify:

- Tool: Claude Code
- Model: Opus
- Thinking: On
- Effort: High
- Workspace: MNL with read-only nested package
- Prompt: full contents of
  `JMP_M05C_incrementA_bounded_refix_prompt_v1.md`
- Return destination: this Goal 1 Manager chat

Do not ask Hisham to choose the tools, model, files, commands, or routing.

Return to the deputy programme director only if:

- Review A v2 returns REJECT;
- a design/package/architecture issue appears;
- an accepted artifact changes;
- a full-population run occurs;
- another E2 halt is triggered.

The production real run remains unauthorized.
