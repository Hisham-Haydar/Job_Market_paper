# Prompt — Delegate JMP-M05C to the Goal 1 Manager

Use in the existing persistent:

`Goal 1 Manager — Empirical JMP`

**Model:** Fable; Opus fallback  
**Thinking:** On  
**Effort:** High / long-horizon management

---

Adopt these as binding:

- `JMP_M05B_pause_and_M05C_redesign_decision_v1.md`
- `JMP_M05C_streaming_inference_design_addendum_v1.md`
- `JMP_M05C_minimal_streaming_implementation_mission_charter_v1.md`

JMP-M05B is closed without implementation acceptance. Do not commission another
repair of its architecture.

First commission the archive, evidence-only commit, rejected-source cleanup, and
test-42 salvage using:

`JMP_M05B_archive_and_test42_salvage_prompt_v1.md`

After the return proves a clean MNL base:

1. open `JMP-M05C — Minimal Streaming Inference Implementation`;
2. create and maintain
   `docs/missions/JMP_M05C_mission_ledger_v1.md`;
3. appoint a fresh Claude Code Opus implementation session that has not worked
   on the rejected M05B implementation;
4. independently create exact operational prompts for Increment A, B, and C
   from the mission charter;
5. after each increment, commission a fresh read-only Codex review;
6. permit at most one narrow remediation for APPROVE AFTER FIXES at each
   increment;
7. stop on any REJECT;
8. require reviewer-runnable production-path proofs and prohibit replacing the
   evaluator/reducer/orchestration under review;
9. do not use or reference the restricted score store;
10. do not persist household-level scores;
11. after final integrated approval, commit the exact reviewed implementation
    and run exactly one full aggregate-only dry run;
12. return to the deputy programme director only with the final audited dry-run
    packet or an E2 halt.

Every operational reply must contain a complete NEXT ACTION CARD. You choose the
tool, model, thinking, effort, workspace, files, exact prompt, outputs, return
destination, and halt conditions. Do not ask Hisham to design the workflow.

Your first NEXT ACTION CARD must use:

- Tool: Claude Code
- Model: Sonnet
- Thinking: On
- Effort: High
- Prompt: full contents of
  `JMP_M05B_archive_and_test42_salvage_prompt_v1.md`
- Return destination: this Goal 1 Manager chat

The production real run remains unauthorized.
