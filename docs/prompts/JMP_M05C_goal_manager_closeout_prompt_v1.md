# Prompt — Goal 1 Manager Closeout of JMP-M05C

Use in the existing persistent:

`Goal 1 Manager — Empirical JMP`

**Model:** Fable; Opus fallback  
**Thinking:** On  
**Effort:** High

---

Adopt as binding:

- `JMP_M05C_deputy_phase5_acceptance_v1.md`;
- `JMP_M05C_W4_routing_memo_v1.md`.

The audited full-population dry-run attempt is the accepted canonical Phase-5
result. Do not commission another numerical run. Do not create `complete/`.

Manage autonomously:

1. commission the MNL evidence promotion, D11 test-only correction, and
   Job_Market_paper documentation checkpoint using
   `JMP_M05C_closeout_and_evidence_commit_prompt_v1.md`;
2. inspect the returned hashes, committed file lists, tests, and repository
   statuses;
3. update and close `JMP_M05C_mission_ledger_v4.md` or create a final v5 only if
   required by the established ledger convention;
4. record W-4 as S-10 Tier 1 and route it to the later M07/M08 workstream;
5. mark the special restricted-score retention/ACL question closed as moot for
   Phase 5;
6. mark the optional post-reset Codex re-verification as not required;
7. issue a concise final closeout record with:
   - evidence commit SHA;
   - D11 commit SHA;
   - Job_Market_paper checkpoint SHA;
   - canonical attempt path;
   - Phase-5 bundle SHA;
   - final test result;
   - repository statuses;
   - confirmation that no second numerical run occurred.

Do not return to the deputy programme director for routine commit mechanics.

After closeout, prepare the next Goal 1 strategic action from the canonical
roadmap. Do not reopen Phase-5 software certification.

Every operational reply must contain a complete NEXT ACTION CARD.

Your first action card must use:

- Tool: Claude Code
- Model: Sonnet
- Thinking: On
- Effort: Medium-high
- Workspaces: MNL and Job_Market_paper
- Prompt: full contents of
  `JMP_M05C_closeout_and_evidence_commit_prompt_v1.md`
- Return destination: this Goal 1 Manager chat
