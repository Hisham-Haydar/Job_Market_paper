# Prompt — Resume JMP-M05B After E2 Decision

Use in the existing persistent:

`Goal 1 Manager — Empirical JMP`

**Model:** Fable; Opus fallback  
**Thinking:** On  
**Effort:** High / long-horizon management

---

Adopt `JMP_M05B_E2_deputy_decision_v1.md` as binding.

The code-review REJECT is valid. The statistical design remains closed.

You are authorized to manage without returning to the deputy programme
director:

1. restricted-store provisioning;
2. bounded remediation cycle 1 of 2 implementing exactly the twelve fixes;
3. deterministic no-full-score validation;
4. independent Codex review v2;
5. one final narrow remediation only if review v2 says APPROVE AFTER FIXES;
6. separate Phase-4 test-42 housekeeping and separate commit;
7. exact reviewed Phase-5 implementation/review commit;
8. one full dry run and Goal 1 audit.

Use these operational instruments:

- `JMP_M05B_restricted_store_provisioning_prompt_v1.md`
- `JMP_M05B_bounded_remediation_prompt_v1.md`
- `JMP_M05B_code_review_v2_prompt_v1.md`
- `JMP_M05B_test42_housekeeping_prompt_v1.md`

Update `JMP_M05B_mission_ledger_v1.md` in your response and at the next planned
documentation checkpoint:

- I-3: REJECT, E2 resolved by deputy;
- I-4: remediation cycle 1 authorized;
- remaining remediation budget: one cycle;
- restricted-store provisioning is a prerequisite for closing fix 8;
- dry run remains blocked until review v2 APPROVE and exact-state commits.

Every reply must contain one complete NEXT ACTION CARD under your operating
contract.

Your first action card must commission restricted-store provisioning using
Claude Code Sonnet, thinking on, medium effort.

After provisioning returns READY or READY WITH RETENTION WARNING, commission
the bounded remediation in the preserved original implementer session using
Claude Code Opus, thinking on, high effort.

Do not ask Hisham to choose paths, tools, prompts, models, effort, files, or
routing beyond executing your action cards.

Return to the deputy programme director only:

- after the audited full dry run with the charter packet; or
- immediately if review v2 returns REJECT, the second remediation fails, the
  store cannot be provisioned, the package must change, or another E2 halt
  occurs.

The production real run remains unauthorized.
