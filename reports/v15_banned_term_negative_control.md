# V15 banned-term audit negative control

Overall: **PASS**

A temporary rendered copy received `S11` rendered HTML immediately before the explicit appendix begin marker. The same scanner was expected to return **FAIL** and returned **FAIL** with 1 detected hit.

The temporary file was removed: **TRUE**. The original rendered report's SHA-256 remained unchanged: **TRUE**.
