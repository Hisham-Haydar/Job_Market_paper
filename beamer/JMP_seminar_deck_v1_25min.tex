% ====================================================================
% The 25-minute running order.
%
% A driver, not a second deck: it sets the switch and \input{}s the same
% JMP_seminar_deck_v1.tex, so there is exactly one source of slides and
% the two variants cannot drift.
%
% The frozen plan (manuscript/JMP_seminar_deck_content_v1.md) reaches 16
% slides from 23 by FIVE cuts and TWO merges:
%   CUT -> backup : 9 (heterogeneity), 12 (draw stability), 17 (geography),
%                   18 (the sex subgroup), 22 (limits)
%   MERGE         : 6+7 (data + pricing), 10+11 (hours + employment/wages)
% 23 - 5 - 2 = 16 slides, 23.0 minutes, 2.0 minutes of slack.
% ====================================================================
\def\ShortDeck{}
\input{JMP_seminar_deck_v1.tex}
